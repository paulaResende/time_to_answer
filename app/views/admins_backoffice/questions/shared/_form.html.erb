<div class="row">
  <div class="col-lg-12">
    <h1 class="page-header"><%= action_message %></h1>
  </div>
  <!-- /.col-lg-12 -->
</div>

<% if @question.errors.any? %>
  <div class="alert alert-danger alert-dismissable">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>

    <ul>
      <% @question.errors.full_messages.each do |question| %>
        <li> <%= message %> </li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="col-lg-6">
  <%= form_with(model: [ :admins_backoffice, @question ], local:true)  do |form| %>
    <div class="form-group">
      <%= form.label :description %>
      <%= form.text_field :description, class:"form-control", placeholder:"Email do administrador" %>
    </div>

    <div class="form-group">
      <%= form.label :subject %>
      <%= form.text_field :subject.description %>

    </div>

    <button type="submit" class="btn btn-default">Salvar</button>
  <% end %>
</div>